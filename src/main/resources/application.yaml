spring:
  application:
    name: ghas-practice
    
  datasource:
    url: jdbc:mysql://localhost:3306/employee_db
    username: appuser
    password: $trongPassword1
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MySQLDialect

server:
  port: 8080
